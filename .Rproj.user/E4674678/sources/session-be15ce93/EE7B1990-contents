# Final project - Hypertension screening Pilot: A process evaluation 

## Overview
This project evaluates a pilot hypertension screening program. The goals are:
1. Identify barriers and facilitators to participation.
2. Assess participant preferences for future program implementation.

## Repository Structure
- `data/` - Raw data of the project.
- `code/` - Scripts to clean data, create table 1, figure 1, and render report.
- `output/` - Generated RDS files and plots.
- `Final_project.Rmd` - Main RMarkdown report.
- `Makefile` - Automates building the report.

## Codes and outputs
The codes above will: 
1. Clean and load the data (load_data.R -> cleaned_data.RDS)
2. Generate Table1 (make_table.R -> table1.RDS)
3. Generate Figure1 (Firgure1.R -> figure1.RDS and graph1.png)
4. Render the final HTML report (render_reprot.R -> Final_project.html)

## How to Generate the Report
From the project root, run:

```bash
make Final_project.html
```

## How to clean outputs
```bash
make clean
```
